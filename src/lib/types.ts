export type { User } from '@/api/users/types';
export type { Appointment } from '@/api/appointments/types';
export type { Task } from '@/api/tasks/types';
export type { Sale } from '@/api/sales/types';
export type { Company } from '@/api/company/types';

export {
  type Role,
  type Department,
  type TaskStatus,
  type SaleStatus,
  ROLES,
  DEPARTMENTS,
  TASK_STATUSES,
  SALE_STATUSES,
} from '@/lib/shared-types';
